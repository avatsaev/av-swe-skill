# Op: `configure`

Show the current resolved config (defaults + overrides). To change a value, rewrite
`swe/av-swe.config.json` (create it if absent) and re-run. Changes take effect on the next op.

Schema, defaults, and project-specific escape hatches: [../conventions/config.md](../conventions/config.md).
